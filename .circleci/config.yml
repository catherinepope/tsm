version: 2.1

orbs:
  node: circleci/node@4.7.0

jobs:      
  run-tests-nosplit:
    docker: 
      - image: cimg/node:15.10.0
    steps:
      - checkout
      - node/install-packages
      - run:
          command: npm run test-ci

workflows:
  Testing-Workflow:
    jobs:
      - run-tests-nosplit
  